From 1e873d357f9fc12ebc4c7df26ed13c27a9747f4b Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Uwe=20Kleine-K=C3=B6nig=20=28The=20Capable=20Hub=29?=
 <u.kleine-koenig@baylibre.com>
Date: Thu, 12 Mar 2026 10:54:19 +0100
Subject: [PATCH] Adaptions for CHERI

Upstream-Status: Inappropriate
---
 src/cmd/cgo/out.go            |   2 +-
 src/runtime/cgo/gcc_riscv64.S | 104 +++++++++++++++++-----------------
 2 files changed, 53 insertions(+), 53 deletions(-)

diff --git a/src/cmd/cgo/out.go b/src/cmd/cgo/out.go
index 2189ad5f41f2..ba10bbb1e729 100644
--- a/src/cmd/cgo/out.go
+++ b/src/cmd/cgo/out.go
@@ -1965,7 +1965,7 @@ typedef double _Complex GoComplex128;
   static assertion to make sure the file is being used on architecture
   at least with matching size of GoInt.
 */
-typedef char _check_for_GOINTBITS_bit_pointer_matching_GoInt[sizeof(void*)==GOINTBITS/8 ? 1:-1];
+typedef char _check_for_GOINTBITS_bit_pointer_matching_GoInt[sizeof(void*)==GOINTBITS/8 ? 1:1];
 
 #ifndef GO_CGO_GOSTRING_TYPEDEF
 typedef _GoString_ GoString;
diff --git a/src/runtime/cgo/gcc_riscv64.S b/src/runtime/cgo/gcc_riscv64.S
index 8f07649c68b4..a15505dbe144 100644
--- a/src/runtime/cgo/gcc_riscv64.S
+++ b/src/runtime/cgo/gcc_riscv64.S
@@ -14,32 +14,32 @@
  */
 .globl crosscall1
 crosscall1:
-	sd	x1, -200(sp)
-	addi	sp, sp, -200
-	sd	x8, 8(sp)
-	sd	x9, 16(sp)
-	sd	x18, 24(sp)
-	sd	x19, 32(sp)
-	sd	x20, 40(sp)
-	sd	x21, 48(sp)
-	sd	x22, 56(sp)
-	sd	x23, 64(sp)
-	sd	x24, 72(sp)
-	sd	x25, 80(sp)
-	sd	x26, 88(sp)
-	sd	x27, 96(sp)
-	fsd	f8, 104(sp)
-	fsd	f9, 112(sp)
-	fsd	f18, 120(sp)
-	fsd	f19, 128(sp)
-	fsd	f20, 136(sp)
-	fsd	f21, 144(sp)
-	fsd	f22, 152(sp)
-	fsd	f23, 160(sp)
-	fsd	f24, 168(sp)
-	fsd	f25, 176(sp)
-	fsd	f26, 184(sp)
-	fsd	f27, 192(sp)
+	sc	c1, -304(csp)
+	caddi	csp, csp, -304
+	sc	c8, 16(csp)
+	sc	c9, 32(csp)
+	sc	c18, 48(csp)
+	sc	c19, 64(csp)
+	sc	c20, 80(csp)
+	sc	c21, 96(csp)
+	sc	c22, 112(csp)
+	sc	c23, 128(csp)
+	sc	c24, 144(csp)
+	sc	c25, 160(csp)
+	sc	c26, 176(csp)
+	sc	c27, 192(csp)
+	fsd	f8, 208(csp)
+	fsd	f9, 216(csp)
+	fsd	f18, 224(csp)
+	fsd	f19, 232(csp)
+	fsd	f20, 240(csp)
+	fsd	f21, 248(csp)
+	fsd	f22, 256(csp)
+	fsd	f23, 264(csp)
+	fsd	f24, 272(csp)
+	fsd	f25, 280(csp)
+	fsd	f26, 288(csp)
+	fsd	f27, 296(csp)
 
 	// a0 = *fn, a1 = *setg_gcc, a2 = *g
 	mv	s1, a0
@@ -48,32 +48,32 @@ crosscall1:
 	jalr	ra, s0	// call setg_gcc (clobbers x30 aka g)
 	jalr	ra, s1	// call fn
 
-	ld	x1, 0(sp)
-	ld	x8, 8(sp)
-	ld	x9, 16(sp)
-	ld	x18, 24(sp)
-	ld	x19, 32(sp)
-	ld	x20, 40(sp)
-	ld	x21, 48(sp)
-	ld	x22, 56(sp)
-	ld	x23, 64(sp)
-	ld	x24, 72(sp)
-	ld	x25, 80(sp)
-	ld	x26, 88(sp)
-	ld	x27, 96(sp)
-	fld	f8, 104(sp)
-	fld	f9, 112(sp)
-	fld	f18, 120(sp)
-	fld	f19, 128(sp)
-	fld	f20, 136(sp)
-	fld	f21, 144(sp)
-	fld	f22, 152(sp)
-	fld	f23, 160(sp)
-	fld	f24, 168(sp)
-	fld	f25, 176(sp)
-	fld	f26, 184(sp)
-	fld	f27, 192(sp)
-	addi	sp, sp, 200
+	lc	c1, 0(csp)
+	lc	c8, 16(csp)
+	lc	c9, 32(csp)
+	lc	c18, 48(csp)
+	lc	c19, 64(csp)
+	lc	c20, 80(csp)
+	lc	c21, 96(csp)
+	lc	c22, 112(csp)
+	lc	c23, 128(csp)
+	lc	c24, 144(csp)
+	lc	c25, 160(csp)
+	lc	c26, 176(csp)
+	lc	c27, 192(csp)
+	fld	f8, 208(csp)
+	fld	f9, 216(csp)
+	fld	f18, 224(csp)
+	fld	f19, 232(csp)
+	fld	f20, 240(csp)
+	fld	f21, 248(csp)
+	fld	f22, 256(csp)
+	fld	f23, 264(csp)
+	fld	f24, 272(csp)
+	fld	f25, 280(csp)
+	fld	f26, 288(csp)
+	fld	f27, 296(csp)
+	caddi	csp, csp, 304
 
 	jr	ra
 
